#include <bits/stdc++.h>
using namespace std;
#define ll long long int
ll t, n, m, k, a, b, c, d, e;
string s, r;
int main()
{
    cin >> s;
    if (s[0] > 'Z')
    {
        s[0] += 'A' - 'a';
    }
    cout << s << endl;
}